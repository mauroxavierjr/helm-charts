apiVersion: v1
kind: Service
metadata:
  name: {{ include "teste-helm.fullname" . }}
  labels:
    {{- include "teste-helm.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "teste-helm.selectorLabels" . | nindent 4 }}

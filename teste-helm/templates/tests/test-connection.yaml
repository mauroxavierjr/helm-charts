apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "teste-helm.fullname" . }}-test-connection"
  labels:
    {{- include "teste-helm.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "teste-helm.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
